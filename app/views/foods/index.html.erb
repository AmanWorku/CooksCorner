

  <div class="foood-container">
    <%= button_to "Add Food", new_food_path, method: :get, class:"add-food" %>
 
        <p class="notice"><%= notice %></p>

        <div class="foods">
        <table class="table">
            <tr>
            <th>Food</th>
            <th>Measurement unit</th>
            <th>Unit price</th>
            <th>Actions</th>
            </tr>
            <% @foods.each do |food| %>
            <%= render food %>
            <% end %>
        </table>
        </div>
 </div>
